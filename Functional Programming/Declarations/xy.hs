-- In un file XY.hs dichiarare due stringhe a e b tali che a è composta
-- dal carattere X seguito da b, mentre b è composta dal carattere Y
-- seguito da a. Verificare che a e b siano dichiarate correttamente
-- usando la funzione take come mostrato in precedenza.

-- FUNCTION DECLARATION
a :: String
a = "X" ++ b

b :: String 
b = "Y" ++ a